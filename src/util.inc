
.code
; Calculate the straight line distance between two chunk coordinates
; AX : Chunk A (X/Y)  
; BX : Chunk B (X/Y)  
; Returns 
; AL : Straight line distance between A and B (number)
DistanceBetweenChunks proc far

    ret
DistanceBetweenChunks endp